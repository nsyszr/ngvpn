# Important changes to server.conf
Following option is required if we're running a plugin that listens to 
auth-user-pass-required. It ensure that no every clients needs to pass 
username/password.

`auth-user-pass-optional`

